## Тест-кейсы и тест-сьюты (Лабораторная работа №1)

Тест кейсы на основе проекта : [«На страже Волшебной школы»](https://github.com/Kirill-Kazantcev/higher-web-practice-school)

### 1. Сьют: Аутентификация и вход в систему (6 кейсов)

| ID | Название | Предусловия | Шаги | Ожидаемый результат |
|:----|:----------|:-------------|:------|:----------------------|
| TC-1 | Вход существующего пользователя (pooh) | Система запущена, пользователь не авторизован | 1. Ввести логин `pooh`<br>2. Ввести пароль `1!` | Вход выполнен, текущий пользователь – Винни Пух (GUARD) |
| TC-2 | Вход с неверным паролем | Система запущена | 1. Ввести логин `owl`<br>2. Ввести неверный пароль `wrong` | Сообщение об ошибке аутентификации |
| TC-3 | Вход несуществующего пользователя | – | 1. Ввести логин `unknown`<br>2. Любой пароль | Ошибка: «Пользователь не найден» |
| TC-4 | Смена текущего пользователя без выхода | Авторизован `pooh` | 1. Выполнить команду `login roo`<br>2. Ввести пароль `5%` | Текущий пользователь сменился на Крошку Ру (STUDENT) |
| TC-5 | Выход из системы (logout) | Авторизован любой пользователь | 1. Выполнить команду `logout` | Сессия завершена, запрос логина и пароля |
| TC-6 | Повторный вход после выхода | Выполнен logout | 1. Войти снова (`rabbit` / `6^`) | Вход успешен, история действий сохранилась |

### 2. Сьют: Ролевая модель и базовые права (5 кейсов)

| ID | Название | Предусловия | Шаги | Ожидаемый результат |
|:----|:----------|:-------------|:------|:----------------------|
| TC-7 | ADMIN имеет полный доступ к помещениям | Авторизован `robin` (ADMIN) | 1. Попытаться войти в `School`, `Cabin`, `Room`, `ClassA` | Все попытки успешны |
| TC-8 | GUARD может войти только в школу | Авторизован `pooh` (GUARD) | 1. Войти в `School` – успех<br>2. Войти в `TeachersRoom` – ошибка | Доступ в школу разрешён |
| TC-9 | STUDENT может войти в класс только с учителем | Авторизован `roo`, учитель `piglet` НЕ в классе | 1. Попытаться войти в `ClassB` | Отказано из-за `TeacherPresentCondition` |
| TC-10 | TEACHER имеет доступ в учительскую и классы | Авторизован `piglet` | 1. Войти в `TeachersRoom`<br>2. Войти в `ClassC` | Оба входа разрешены |
| TC-11 | CHIEF может войти в кабинет директора всегда | Авторизован `owl` | 1. Войти в `DirectorCabin` | Успешно |

### 3. Сьют: Доступ к помещениям (локациям) (6 кейсов)

| ID | Название | Предусловия | Шаги | Ожидаемый результат |
|:----|:----------|:-------------|:------|:----------------------|
| TC-12 | Вход в школу для всех ролей | Все роли поочерёдно | 1. Каждый пробует войти в `School` | Все успешно входят |
| TC-13 | Вход учителя в кабинет директора при отсутствии директора | Директор не в кабинете, авторизован `tigger` | 1. Войти в `DirectorCabin` | Успешно (`DirectorAbsentCondition`) |
| TC-14 | Вход учителя в кабинет директора при присутствии директора | Директор в кабинете, авторизован `eeyore` | 1. Войти в `DirectorCabin` | Отказано |
| TC-15 | Вход студента в класс без учителя | В `ClassA` нет учителя, авторизован `roo` | 1. Войти в `ClassA` | Отказано |
| TC-16 | Вход студента в класс с учителем | Учитель `rabbit` вошёл в `ClassD` | 1. `roo` входит в `ClassD` | Успешно |
| TC-17 | Вход родителя в учительскую | Авторизована `kanga` | 1. Попытаться войти в `TeachersRoom` | Отказано |

### 4. Сьют: Журнал оценок (7 кейсов)

| ID | Название | Предусловия | Шаги | Ожидаемый результат |
|:----|:----------|:-------------|:------|:----------------------|
| TC-18 | Учитель просматривает журнал | Авторизован `piglet` | 1. `watch journal` | Отображаются оценки всех учеников |
| TC-19 | Учитель редактирует журнал | Авторизован `piglet` | 1. Выставить оценку `roo` – 5 | Оценка сохранена |
| TC-20 | Студент просматривает журнал (с учителем) | Учитель в классе, авторизован `roo` | 1. `watch journal` | Студент видит оценки |
| TC-21 | Студент пытается редактировать журнал | Авторизован `roo` | 1. `edit journal` | Отказано |
| TC-22 | Родитель смотрит журнал – только своего ребёнка | Авторизована `kanga` | 1. `watch journal` | Видит только оценки `roo` |
| TC-23 | Родитель пытается редактировать журнал | Авторизована `kanga` | 1. `edit journal` | Отказано |
| TC-24 | Директор просматривает журнал | Авторизован `owl` | 1. `watch journal` | Видит оценки всех учеников |

### 5. Сьют: Условные разрешения (Conditions) (5 кейсов)

| ID | Название | Предусловия | Шаги | Ожидаемый результат |
|:----|:----------|:-------------|:------|:----------------------|
| TC-25 | `DirectorAbsentCondition` – учитель в кабинет, директор отсутствует | Директор не в кабинете, авторизован `rabbit` | 1. Войти в `DirectorCabin` | Успешно |
| TC-26 | `DirectorAbsentCondition` – учитель в кабинет, директор присутствует | Директор в кабинете, учитель пытается войти | 1. `owl` в `Cabin`<br>2. `rabbit` пытается войти | Отказано |
| TC-27 | `TeacherPresentCondition` – студент в класс, учитель есть | Учитель в классе, авторизован `roo` | 1. `roo` входит в класс | Успешно |
| TC-28 | `TeacherPresentCondition` – студент в класс, учителя нет | Учителя нет в классе | 1. `roo` пытается войти | Отказано |
| TC-29 | `OwnChildOnlyCondition` – родитель видит только своего ребёнка | `kanga` | 1. `watch journal` | Только оценки `roo` |

### 6. Сьют: История действий (Accounting) (4 кейса)

| ID | Название | Предусловия | Шаги | Ожидаемый результат |
|:----|:----------|:-------------|:------|:----------------------|
| TC-30 | Запись входа в систему | – | 1. Войти как `pooh` | В истории: «pooh вошёл в систему» |
| TC-31 | Запись перемещения между локациями | Авторизован `tigger` | 1. Войти в `School`<br>2. Перейти в `TeachersRoom` | История содержит оба входа |
| TC-32 | Запись действий с журналом | Авторизован `rabbit` | 1. `watch journal`<br>2. `edit journal` | История: просмотр, редактирование |
| TC-33 | Просмотр истории командой | Любой пользователь | 1. `history` | Выводится список всех действий |

### 7. Сьют: Многопользовательский режим и состояние (5 кейсов)

| ID | Название | Предусловия | Шаги | Ожидаемый результат |
|:----|:----------|:-------------|:------|:----------------------|
| TC-34 | Сохранение местоположения пользователей | `piglet` в `ClassA`, `roo` в `School` | 1. `logout`<br>2. Войти как `owl` | Состояние сохранилось |
| TC-35 | Два пользователя в одной локации | `piglet` в `ClassA` | 1. `tigger` входит в `ClassA` | Оба в `ClassA` |
| TC-36 | Пользователь покидает локацию | `piglet` в `ClassA` | 1. `leave ClassA` | Пользователь покинул класс |
| TC-37 | Проверка присутствия для условия | Учитель `piglet` в `ClassA` | 1. `who is in ClassA` (гипотетическая команда) | Возвращает `piglet` |
| TC-38 | Смена пользователя без потери состояния | `piglet` в `ClassA` | 1. `login roo` | `roo` текущий, `piglet` остался в классе |

### 8. Сьют: Граничные случаи и ошибки (Edge Cases) (5 кейсов)

| ID | Название | Предусловия | Шаги | Ожидаемый результат |
|:----|:----------|:-------------|:------|:----------------------|
| TC-39 | Вход в несуществующую локацию | Авторизован любой | 1. `enter Dungeon` | «Локация не найдена» |
| TC-40 | Редактирование журнала без права | Авторизован `roo` | 1. `edit journal` | Отказ |
| TC-41 | Пустой ввод команды | – | 1. Нажать Enter | Приглашение повторяется |
| TC-42 | Выход из локации, где пользователя нет | `pooh` в `School` | 1. `leave ClassA` | «Вы не находитесь в ClassA» |
| TC-43 | Повторный вход в ту же локацию | `pooh` в `School` | 1. `enter School` | «Вы уже в школе» |

### 9. Сьют: Интеграционные сценарии (4 кейса)

| ID | Название | Предусловия | Шаги | Ожидаемый результат |
|:----|:----------|:-------------|:------|:----------------------|
| TC-44 | Учитель ставит оценку, студент видит | Авторизованы `piglet`, затем `roo` | 1. `piglet` в `ClassA`<br>2. Ставит 5 `roo`<br>3. `roo` в `ClassA`<br>4. `roo` смотрит журнал | Видит 5 |
| TC-45 | Директор в кабинете, учитель не входит | `owl` в `Cabin` | 1. `rabbit` пытается войти | Отказ |
| TC-46 | Родитель смотрит оценки | Учитель выставил оценки | 1. `kanga` → `watch journal` | Только оценки `roo` |
| TC-47 | Администратор смотрит историю | Несколько пользователей действовали | 1. `robin` → `history` | Видит всю историю |

### 10. Сьют: Проверка данных пользователей по умолчанию (3 кейса)

| ID | Название | Предусловия | Шаги | Ожидаемый результат |
|:----|:----------|:-------------|:------|:----------------------|
| TC-48 | Привязка родителя к ребёнку | `kanga` | 1. Посмотреть `childrenNames` | Список содержит `roo` |
| TC-49 | Все 9 пользователей загружены | Система запущена | 1. Войти под каждым из списка | Каждый успешно входит |
| TC-50 | Роли соответствуют описанию | Все пользователи поочерёдно | 1. Проверить разрешения согласно документации | Поведение соответствует ролевой модели |

---
